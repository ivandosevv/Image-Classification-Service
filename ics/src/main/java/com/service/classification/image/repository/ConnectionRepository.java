package com.service.classification.image.repository;

import com.service.classification.image.data.Connection;
import com.service.classification.image.data.ConnectionKey;

public interface ConnectionRepository extends Repository<Connection, ConnectionKey>{
}
